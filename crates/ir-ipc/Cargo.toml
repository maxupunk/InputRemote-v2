[package]
name = "ir-ipc"
description = "O contrato entre o serviço, o agente e a interface."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ir-proto.workspace = true
postcard.workspace = true
serde.workspace = true
thiserror.workspace = true

# So no Windows: o cliente do canal abre o named pipe com E/S sobreposta, e o `tokio` e quem faz
# isso sem `unsafe` neste crate. Sem ele, uma thread presa numa leitura trava a escrita de outra
# (ver `src/cliente.rs`). No Linux o socket Unix da biblioteca padrao basta.
[target.'cfg(windows)'.dependencies]
tokio.workspace = true

[lints]
workspace = true
